// Parent tag value used to namespace auto-applied zero-shot CLIP tags.
// Tags are stored hierarchically as `auto/<label>`, so users can filter
// out all auto-tags at once or remove the parent to clear them.
export const ZERO_SHOT_TAG_NAMESPACE = 'auto';
