--- a/image_description_prompt_config.dart
+++ b/image_description_prompt_config.dart
@@ -22,7 +22,7 @@
     this.medicalIndicators = const Optional.present(const []),
     this.nsfwIndicators = const Optional.present(const []),
     this.sentenceCountTarget = const Optional.present(3),
-    this.style = const Optional.present(const ImageDescriptionPromptConfigStyleEnum._('balanced')),
+    this.style = const Optional.present(ImageDescriptionPromptConfigStyleEnum.balanced),
   });
 
   ///
